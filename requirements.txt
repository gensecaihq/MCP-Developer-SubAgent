# Core dependencies for Claude Code MCP SDK
anthropic>=0.20.0
python-dotenv>=1.0.0
pydantic>=2.0.0
click>=8.0.0
aiofiles>=0.8.0

# Optional MCP framework (install manually if needed)
# fastmcp>=0.1.0  # Commented out - may not be available on PyPI

# Authentication dependencies (optional)
pyjwt>=2.0.0

# Development tools (optional)
black>=22.0.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
mypy>=1.0.0
coverage>=7.0.0